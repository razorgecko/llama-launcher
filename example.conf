# Example profile. Copy this and edit for each model you use.
# Lines starting with # are ignored. Each non-empty line becomes an argument
# passed to llama-server (or sd-server for Stable Diffusion profiles).

# --- Meta (controls how this profile appears in the UI) ---
# name: My Model
# description: Optional description shown under the name
# type: llama   # or: sd  (for stable-diffusion.cpp)

--model /path/to/your-model.gguf
--ctx-size 8192
--n-gpu-layers 99
--port 8080
--host 127.0.0.1
