# Example profile. Copy this and edit for each model you use.
# Lines starting with # are ignored. Each non-empty line becomes arguments
# passed to llama-server.

--model /path/to/your-model.gguf
--ctx-size 8192
--n-gpu-layers 99
--port 8080
--host 127.0.0.1
